name: My Docker Workflow1


on:
  push:
    branches:
      - main

jobs:
  build-java-project1:
    runs-on: ubuntu-latest
    steps:
        - uses: actions/checkout@v2

        - run: |
             docker build -t temp-linter .
             docker images
        

        
