on:
  workflow_dispatch:
  push:
  
  
jobs:
  Hello:
    runs-on: ubuntu-latest
    env:
      cache-name: my-cache-name  # Define cache-name environment variable
    steps:
      
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
      -
        name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: prathira
          password: dckr_pat_YC40q1ZsK_cLlxe5AXgSQSzKNK4
      - name: Restore Super-Linter cache
        uses: actions/cache@v2
        with:
          path: /var/lib/docker
          key: ${{ runner.os }}-docker-Rajesh1
      - name: Pull Super Linter Docker image
        if: env.CACHE_CREATED != 'true'
        run: docker pull github/super-linter:v4
      - name: Cache Docker image
        if: env.CACHE_CREATED != 'true'
        uses: actions/cache@v2
        with:
          path: /var/lib/docker
          key: ${{ runner.os }}-docker-Rajesh1
          restore-keys: ${{ runner.os }}-docker-Rajesh1
        env:
           CACHE_CREATED: true
